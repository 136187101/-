   如果是sum as 一个在数据库里没有的字段 则需要在model类里面 追加 新的字段属性 不然 sum的新字段无法显示
 
追加属性如下：
public function attributes()
    {
         $arr=parent::attributes(); // TODO: Change the autogenerated stub
         $arr[]='hjd';
         return $arr;
    }